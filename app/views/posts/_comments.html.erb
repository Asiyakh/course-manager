<% comments.each do |comment|%>
    <div class="post-comments">
        <%= link_to comment.account.full_name, profile_path(comment.account.utorid)%>
        <%= simple_format comment.message %>
    </div>
<% end %>